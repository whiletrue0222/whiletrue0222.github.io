# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.

# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.

domain: whiletrue0222.github.io

title: whiletrue
email: whiletrue0222@gmail.com
description: "" # this means to ignore newlines until "baseurl:"
baseurl: "" # the subpath of your site, e.g. /blog
url: "" # the base hostname & protocol for your site, e.g. http://example.com
twitter_username: whiletrue0222
github_username:  whiletrue0222

# Build settings
markdown: kramdown
theme: jekyll-theme-slate
plugins:
  - jekyll-feed
  - jekyll-github-metadata

repository: whiletrue0222/whiletrue0222.github.io
timezone: Asia/Seoul

# 여기는 프로젝트 페이지가 아니라 사용자 페이지다. jekyll-github-metadata 는 이 값을
# API 응답으로 추론하는데 인증이 없으면 true 로 잘못 잡혀서, slate 테마의
# "View on GitHub" 배너와 푸터가 원래 없던 자리에 붙는다. 명시적으로 고정한다.
github:
  is_project_page: false

# 클래식 GitHub Pages 빌드가 테마 레이아웃을 자동으로 씌워줬기 때문에 index.md 에는
# layout 이 없다. 순정 Jekyll 은 주입해주지 않으므로 여기서 기본값을 지정한다.
defaults:
  - scope:
      path: ""
    values:
      layout: default

# Exclude from processing.
# The following items will not be processed, by default. Create a custom list
# to override the default setting.
exclude:
  - Gemfile
  - Gemfile.lock
  - README.md
  - mise.toml
  - vendor
#   - node_modules
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/
